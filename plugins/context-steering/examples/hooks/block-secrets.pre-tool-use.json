{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if grep -Eiq 'AKIA[0-9A-Z]{16}|password[[:space:]]*='; then echo 'Potential secret detected; write blocked' >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
