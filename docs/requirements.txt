# Documentation site (MkDocs Material). Install with:
#   uv run --with-requirements docs/requirements.txt mkdocs serve
# or
#   pip install -r docs/requirements.txt
#
# Markdown is pinned explicitly: pymdown-extensions < 11 (e.g. 10.15) silently
# breaks superfences code blocks on Markdown >= 3.10, so keep this trio in sync.
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
pymdown-extensions==11.0.1
Markdown==3.10.2
